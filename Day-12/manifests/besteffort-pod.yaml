apiVersion: v1
kind: Pod
metadata:
  name: besteffort-pod
  namespace: default
  labels:
    app: besteffort-demo
spec:
  containers:
  - name: processor
    image: alpine:latest
    command: ["sleep", "3600"]
    # No resources block defined, making it BestEffort QoS

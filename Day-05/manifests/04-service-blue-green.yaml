apiVersion: v1
kind: Service
metadata:
  name: payment-processor-bg-svc
  namespace: default
  labels:
    app.kubernetes.io/name: payment-processor-bg
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: payment-processor-bg
    color: blue

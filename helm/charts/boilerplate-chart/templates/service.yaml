apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-boilerplate
  labels:
    app: {{ .Release.Name }}-boilerplate
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-boilerplate
